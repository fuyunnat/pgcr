#ifndef SCREEN_PROBE_H
#define SCREEN_PROBE_H

/* Read-only QNX Screen inventory. Does not create, move, resize, join,
 * destroy, or otherwise mutate existing vehicle windows. */
int pgcr_run_screen_probe();

#endif
